import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SequenceCreateScreen = () => {
   return (
      <View>
         <Text>Sequence Create Screen</Text>
      </View>
   )
};

const styles = StyleSheet.create({});

export default SequenceCreateScreen;